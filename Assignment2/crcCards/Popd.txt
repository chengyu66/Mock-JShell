Class name: Popd
Parent class (if any):Command
Subclasses (if any):

Responsibilities:
* Remove the top entry from the directory stack
* changes the new current directory to the top entry of directory stack

Collaborators:
* DirectoryStack
* FileSystem
